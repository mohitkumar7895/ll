# Local dev: use Vite proxy (recommended)
VITE_API_BASE_URL=/api

# Or point straight to backend (must add that origin to server CORS + use same http/https)
# VITE_API_BASE_URL=http://localhost:5000/api
