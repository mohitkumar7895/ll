/** No automatic seats — admin creates inventory from the dashboard. */
const seedDefaultSeats = async () => {
  console.log("Seat seed skipped (default inventory is empty; admin adds seats).");
};

module.exports = seedDefaultSeats;
